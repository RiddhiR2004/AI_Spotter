<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow user-installed certificates (for development on restrictive networks) -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <!-- Trust preinstalled CAs -->
            <certificates src="system" />
            <!-- Additionally trust user-installed CAs (for corporate/college networks) -->
            <certificates src="user" />
        </trust-anchors>
    </base-config>
    
    <!-- Debug configuration for local development -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>

